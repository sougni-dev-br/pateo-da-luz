node_modules/
dist/
build/
pateo-dist.zip
frontend/dist-deploy.zip
HANDOFF.md

# Environment files — nunca commitar segredos reais
.env
.env.local
.env.*
!.env.example
!frontend/.env.production

*.log
*.err
tmp-*
uploads/
backend/uploads/
backend/dist/
frontend/dist/
.DS_Store
Thumbs.db
.vscode/
.idea/
*.tsbuildinfo
.cache/

# Scripts temporários e dados de produção na raiz
/*.sql
/*_prod.json
/dre_*
/.claude/

# Scripts de diagnóstico/manutenção com dados de produção
backend/prisma/scripts/

# Dumps de banco e exports com dados sensíveis
exports/database/
*.dump

agent-pdv.zip
agent-pdv (*).zip

# Reports gerados pelos scripts CMV — não versionar (contém snapshots de dados)
exports/

# Documentação NDA do 99 Food (Sob acordo de confidencialidade da 99/DiDi).
# Ficam disponíveis localmente pra referência de desenvolvimento, mas
# NUNCA vão pro repositório.
docs/99food/swagger.yaml
docs/99food/*.pdf
docs/99food/private/

# Rascunhos de sessão — a convenção do projeto é prefixo "_" na raiz e no
# backend (_audit.bat, _test_import.ts, _extrato_teste.pdf...). Entre eles
# ficam PDFs do Extrato Mensal do RH, com nome, CPF e líquido de cada
# funcionário: um "git add ." distraído levaria folha de pagamento pro
# repositório. Nenhum arquivo rastreado casa com estas regras.
/_*
/backend/_*

# CSVs operacionais gerados pelos scripts (recontagem, exports pontuais).
# Carregam dados de estoque e custo; ficam locais.
/*.csv

# Pacotes de deploy do frontend gerados na raiz.
/pateo-frontend-dist.zip
